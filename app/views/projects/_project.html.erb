<div class="project">
  <div class="title">  
    <span class="col-md-1">   
      <%= link_to_if false, content_tag('span'," ", class: 'fa fa-sticky-note'), '#' %>
    </span>  
    <span class="col-md-9">
      <strong><%= project.name %></strong>  
    </span >
    <span class="col-md-2">
      <%= link_to " ", edit_project_path(project), class: 'fa fa-pencil action' %>
      <%= link_to "", project, method: :delete, data: { confirm: "You sure?" }, title: project.name, class: 'fa fa-trash-o action' %>     
    </span>
  </div>
  <%= render 'tasks/task_form', project: project, task: @task%>
  <%= render 'tasks/tasks', project: project %>
  <div class="clearfix"></div>
</div>